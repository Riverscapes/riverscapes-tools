{
  "$schema": "../config.schema.json",
  "profile_name": "US Contiguous States (CONUS)",
  "description": "Standard RS Context Neo configuration for the contiguous United States and Alaska. Uses USGS 3DEP 1 m DEM tiles via The National Map tile API, with optional transportation layers (roads and rail) from AWS S3 Tables via Athena. Set the required environment variables in your .env file before running.",
  "download_dir": "{env:DOWNLOAD_DIR}",
  "scratch_dir": "{env:SCRATCH_DIR}",

  "dem": {
    "source": "tnm",
    "aoi": "{env:AOI_GEOJSON_PATH}",
    "resolution": 1.0,
    "tnm_options": {
      "download_workers": 4,
      "buffer_deg": 0.01
    }
  },

  "hydrology": {
    "threshold": 50000,
    "breach_dist": 100,
    "cores": null
  },

  "layers": [
    {
      "id": "LANDFIRE_EVT",
      "label": "Existing Vegetation",
      "type": "raster",
      "input": "{env:LANDFIRE_EVT_PATH}",
      "output_path": "vegetation/existing_veg.tif",
      "source_url": "https://landfire.gov/data_overviews.php",
      "data_product_version": "2.4.0",
      "docs_url": "https://tools.riverscapes.net/rscontext/data/#EXVEG",
      "description": "LandFire Existing Vegetation Type (EVT) raster: Complexes of plant communities representing NatureServe's terrestrial Ecological Systems classification. The source is a single raster for the entire nation that is downloaded and then clipped to the riverscapes context project extent. This raster has the same raster properties and data extent as the DEM."
    },
    {
      "id": "TRANSPORTATION",
      "label": "Transportation",
      "type": "s3tables",
      "output_path": "transportation/transportation.gpkg",
      "athena_output": "{env:ATHENA_OUTPUT}",
      "sublayers": [
        {
          "layer_name": "roads",
          "s3tables_path": "{env:ROADS_S3TABLES_PATH}"
        },
        {
          "layer_name": "rail",
          "s3tables_path": "{env:RAIL_S3TABLES_PATH}"
        }
      ]
    }
  ],

  "metadata": {}
}
