query projectFiles ($projectId: ID!) {
    project(id: $projectId) {
        files {
            size
            contentType
            downloadUrl
            localPath
            etag
        }
        id
    }
}