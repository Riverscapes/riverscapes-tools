query requestUploadImage_queries(
  $entityId: ID!
  $entityType: EntitiesWithImagesEnum!
) {
  requestUploadImage(
    entityId: $entityId
    entityType: $entityType
  ) {
    fields
    token
    url
  }
}
